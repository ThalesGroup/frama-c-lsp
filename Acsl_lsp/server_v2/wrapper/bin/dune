; (include_subdirs unqualified)

; (rule
; 	(alias frama-c-configure)
; 	(deps (universe))
; 	(action (progn
; 			(echo "acsl_lsp:" %{lib-available:frama-c-acsl_lsp.core} "\n")
; 			(echo " - Frama-C:" %{lib-available:frama-c.kernel} "\n")
; 		)
; 	)
; )

(executable
 (public_name wrapper)
 (name "main")
 (modes plugin exe)
	(flags -open Frama_c_kernel :standard -w -33)
 (libraries 
  frama-c.kernel
  frama-c.init.cmdline
  frama-c-acsl_lsp.core
 )
)

; (library
;  (name wrapper)
;  )
